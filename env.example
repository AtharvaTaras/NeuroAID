# NeuroAID Environment Variables
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control

# Authentication Credentials
# These are used for the login system in the Streamlit app
USERNAME=your_username_here
PASSWORD=your_password_here
NAME=Your Full Name

# AWS S3 Credentials
# Required for storing patient records and scan results
# If you don't have AWS credentials, you'll need to create an S3 bucket and IAM user
AWS_ACCESS_KEY_ID=your_aws_access_key_id_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key_here

# Optional: AWS Region (defaults to us-east-1 if not specified)
# AWS_DEFAULT_REGION=us-east-1

# Optional: S3 Bucket Name (if different from 'neuroaid')
# S3_BUCKET_NAME=neuroaid

